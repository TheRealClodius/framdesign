{
  "buildCommand": "npm run build:tools && npm run build",
  "outputDirectory": ".next",
  "framework": "nextjs",
  "installCommand": "npm install && rm -rf node_modules/@lancedb",
  "_comment": "buildCommand generates tools/tool_registry.json before Next.js build, then copies it to .next/server/tools/ to ensure it's included in serverless function bundles"
}

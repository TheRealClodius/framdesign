{
  "toolId": "perplexity_search",
  "version": "1.0.0",
  "category": "retrieval",
  "description": "Search the web for real-time information using Perplexity AI. Use this when you need current information, recent events, or facts not in the knowledge base.",
  "parameters": {
    "type": "object",
    "additionalProperties": false,
    "required": ["query"],
    "properties": {
      "query": {
        "type": "string",
        "description": "Search query or question to find real-time information",
        "minLength": 3,
        "maxLength": 500
      }
    }
  },
  "sideEffects": "read_only",
  "idempotent": true,
  "requiresConfirmation": false,
  "allowedModes": ["text", "voice"],
  "latencyBudgetMs": 5000
}

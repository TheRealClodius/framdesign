{
  "toolId": "kb_get",
  "version": "1.0.0",
  "category": "retrieval",
  "description": "Retrieve complete KB document by exact ID. Faster than search when entity ID is known. Returns full document with all metadata.",
  "parameters": {
    "type": "object",
    "additionalProperties": false,
    "required": ["id"],
    "properties": {
      "id": {
        "type": "string",
        "description": "Exact entity ID from kb_search results. NEVER construct or guess IDs - always use the 'id' field from kb_search response.",
        "minLength": 3,
        "maxLength": 100,
        "pattern": "^[a-z_]+:[a-zA-Z0-9_]+$"
      }
    }
  },
  "sideEffects": "read_only",
  "idempotent": true,
  "requiresConfirmation": false,
  "allowedModes": ["voice", "text"],
  "latencyBudgetMs": 500
}
